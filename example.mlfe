module math

export add/2
export sub/2

adder x y = x + y

add x y = adder x y

(* I think the assertions should actually get imported/defined
 * by a library, where a test case in the list really returns
 * either "passed" or "failed and here's a message why".
 *)
-test add = [assert_equal (add 2 2) 4,
             assert_equal (add 1 5) 6]

sub x y = x - y

-test sub = [assert_equal (sub 5 1) 4,
             assert_equal (sub 2 4) -2]
             
do_math x y math_fun = math_fun x y

